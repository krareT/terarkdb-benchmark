This the test framework based on leveldb db_bench.
The framework will support storage engines as follows:
LevelDB		(Done)		version 1.18  	test
RocksDB		(Done) 		version 4.1  	test
Redis		(Done)  	version 3.0.6 	test
Wiredtiger	(Done) 		version 2.7.0 	test
TerarkDB	(Done)  	version 0.1.1   test

These test codes are in doc, there are five workloads:
1. bench (default)
2. movies (from amazon)
3. pagecounts (from wiki)
4. humangenome 
5. wikiarticles (from wiki)

Now, the test framework supports fillrandom,readrandom,readwhilewriting benchmarks for each dbs, and we will implement other benchmarks.

## How to build db_bench_* (e.g. db_bench_terark)

1. modify Makefile to add "TERARKDB_INCLUDE_PATH", and TERARKDB_INCLUDE_PATH includes nark-db/src

2. create symlink: libTerarkDB-g++-5.3-r.so, libterark-fsa_all-g++-5.3-r.so in /usr/lib64

3. run "make db_bench_terark"

## How to run db_bench_terark
for example: ./db_bench_terark --benchmarks=fillrandom,readrandom --value_size=64 --num=10000 --db=data
